def upsertion(*args, **kwargs) -> None:
    """Performs an efficient upsertion step into the database using the links from the `link` step.

    This function's arguments and return type will be highly dependent on the specifics of your database schema and the way you've structured your entity linking outputs.
    """
