def validate_schema(schema_object: dict, schema_spec: dict) -> bool:
    """Validates a schema object against a schema specification.

    Args:
        schema_object (dict): The schema object to validate.
        schema_spec (dict): The schema specification to validate against.

    Returns:
        bool: True if the schema object adheres to the schema specification, False otherwise.
    """
